

const AiImage = () => {
  return (
    <div className="animate-fade-in glass-card">
      <h2 className="text-gradient" style={{ marginBottom: '1rem' }}>Công cụ Tạo Ảnh AI Tùy Chỉnh</h2>
      <p className="text-secondary">Tính năng này đang được xây dựng (cần kết nối API)...</p>
    </div>
  );
};

export default AiImage;
